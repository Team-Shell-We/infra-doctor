# Test Project

Copy any Spring Boot (Gradle) project into this directory for local testing.

Before committing, remove:

- `.git`
- `.gradle`
- `.idea`
- `build`

This project is ignored for production use and exists only for development and testing.